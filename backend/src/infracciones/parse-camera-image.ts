// OCR DESHABILITADO: stub para mantener compatibilidad de imports.
// Si nadie lo importa, podés borrar este archivo.

export async function parseCameraImage(_filePath: string) {
  return { ocr: {}, anpr: {} };
}
